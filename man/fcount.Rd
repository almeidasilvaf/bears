% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_03_quantification_featureCounts.R
\name{fcount}
\alias{fcount}
\title{Count reads with featureCounts}
\usage{
fcount(
  sample_info = NULL,
  mappingdir = "results/04_read_mapping",
  gff_path = NULL,
  fcountsdir = "results/05_quantification/featureCounts",
  threads = 2,
  envname = NULL,
  miniconda_path = NULL
)
}
\arguments{
\item{sample_info}{Data frame of sample metadata created with the
functions \code{create_sample_info} and \code{infer_strandedness}.}

\item{mappingdir}{Directory where .bam files are stored.}

\item{gff_path}{Path to GFF/GTF file with annotations.}

\item{fcountsdir}{Directory where the matrix of gene-level read counts will
be stored.}

\item{threads}{Number of threads for featureCounts. Default: 2.}

\item{envname}{Name of the Conda environment with external dependencies
to be included in the temporary R environment.}

\item{miniconda_path}{Path to miniconda. Only valid if envname is specified.}
}
\value{
A gene expression matrix with genes in row names and samples in
column names. Two .tsv files with the gene expression matrix and count stats
are saved to \code{fcountsdir}.
}
\description{
Count reads with featureCounts
}
\examples{
data(sample_info)
mappingdir <- system.file("extdata", package="bears")
gff_path <- system.file("extdata", "Homo_sapiens.GRCh37.75_subset.gtf",
                        package="bears")
fcountsdir <- tempdir()
if(subread_is_installed()) {
    counts <- fcount(sample_info, mappingdir, gff_path, fcountsdir)
}
}
